version: "3.5"

services:
  contrataciones_estado:
    build: ./src
    restart: no
    volumes:
      - ./config/:/config
      - ./inputs/:/inputs
      - ./outputs/:/outputs
      - ./archive/:/archive
      - ./log/:/log
    networks:
      - networkCommon

networks:
  networkCommon:
    driver: bridge
    name: networkCommon
